/**
 * Add connected hook to connected_hooks table
 */
INSERT INTO
  channels
VALUES
  /**
   * webhook_url: VARCHAR(200),
   * connected_at: DATETIME,
   * connected_by: VARCHAR(18),
   * guild_id: VARCHAR(18),
   * connected_channel_id: VARCHAR(18),
   * global_channel_id: VARCHAR(10)
   */
  (?, ?, ?, ?, ?)
